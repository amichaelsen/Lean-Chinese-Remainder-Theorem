import data.fin 
import data.finset 

open fin finset

variables (α : ℕ) 

--could have a number of congruences and then a function 
-- on range n that spits out the a_i and m_i? 

lemma list_input (n : ℕ) (fa : fin n → ℕ) (fm : fin n → ℕ) : false := 
begin
    sorry,
end


lemma list_input (a_i : finset α) : false := 
begin
    sorry,
end